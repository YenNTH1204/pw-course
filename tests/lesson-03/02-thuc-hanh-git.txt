folder demo
a : Tạo 3 file : file1, file2, file3
- Local : file1, file2, file3
- Working directory : 
- Staging Area :
- Repository :

b : Chạy lệnh git init :
- Local repository: đã được khởi tạo, Git tạo thư mục ẩn `.git`
- Working Directory: `file1`, `file2`, `file3` đang ở trạng thái Untracked
- Staging Area: chưa có file nào
- Repository: chưa có commit nào


c : Chạy lệnh git commit -m"init project" :
- Local :
- Working directory : `file1`, `file2`, `file3` vẫn ở trạng thái Untracked
- Staging Area : Không có file nào
- Repository : chưa có commit nào

d : Chạy lệnh git add file1 :
- Local :
- Working directory : `file2`, `file3` vẫn ở trạng thái Untracked
- Staging Area : file1
- Repository : chưa có commit nào

e : Chạy lệnh git commit -m"add file" :
- Local :
- Working directory : `file2`, `file3` vẫn ở trạng thái Untracked
- Staging Area : không có file nào
- Repository : file1

f : Chạy lệnh git add . :
- Local :
- Working directory : 
- Staging Area : file2, file3
- Repository : file1

g : Chạy lệnh git restore --staged file2 :
- Local :
- Working directory : file2
- Staging Area : file3
- Repository : file1

h : Chạy lệnh git commit -m"add all files" :
- Local :
- Working directory : file2
- Staging Area : 
- Repository : file1,file3

i : Chạy lệnh git reset HEAD~1 :
- Local :
- Working directory : file2, file3
- Staging Area : 
- Repository : file1